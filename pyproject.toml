[project]
name = "cross-suggest"
version = "0.1.0"
description = "A cross-media recommendation engine for movies, shows, books, and games."
authors = [
    { name = "Harshad MP", email = "harshadpawaredu@gmail.com" },
]
readme = "readme.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.2",
]

# Note: pandas is not used in the application logic (app.py)
# It was likely added for data exploration but is not a runtime dependency.
